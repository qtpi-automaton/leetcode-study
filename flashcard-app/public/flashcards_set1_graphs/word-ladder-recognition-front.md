**Front (The Prompt):**
* **Problem:** Shortest transformation sequence length from beginWord to endWord, changing one letter at a time. Example: `"hit"→"cog"` via `["hot","dot","dog","lot","log","cog"]` → `5`
* **Key Constraints:** word.length ≤ 10, wordList.length ≤ 5000
