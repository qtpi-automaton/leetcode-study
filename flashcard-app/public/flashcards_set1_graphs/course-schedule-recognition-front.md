**Front (The Prompt):**
* **Problem:** Can finish all courses given prerequisites? Example: `numCourses=2, prerequisites=[[1,0]]` → `true`; `[[1,0],[0,1]]` → `false`
* **Key Constraints:** numCourses ≤ 10^5, prerequisites.length ≤ 5000
