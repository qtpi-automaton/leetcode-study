**Front (The Prompt):**
* **Problem:** Design key-value store with timestamps. `set(key, value, timestamp)` and `get(key, timestamp)` returns value at largest timestamp ≤ given timestamp.
* **Key Constraints:** Up to 2×10⁵ calls, timestamps strictly increasing for set
