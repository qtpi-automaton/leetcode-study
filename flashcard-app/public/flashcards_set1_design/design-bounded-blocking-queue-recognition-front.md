**Front (The Prompt):**
* **Problem:** Thread-safe bounded queue: enqueue blocks when full, dequeue blocks when empty. Multiple producer/consumer threads.
* **Key Constraints:** 1 ≤ producers, consumers ≤ 8, capacity ≤ 30
