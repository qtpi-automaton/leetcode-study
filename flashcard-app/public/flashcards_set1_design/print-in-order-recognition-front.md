**Front (The Prompt):**
* **Problem:** Three threads call first(), second(), third() concurrently. Ensure execution order: first → second → third. Example: input `[1,3,2]` → output "firstsecondthird"
* **Key Constraints:** Threads scheduled in any order, must enforce sequence
