**Front (The Prompt):**
* **Problem:** Given n×n binary matrix, return length of shortest clear path from top-left to bottom-right. Clear path uses only 0s, allows 8-directional movement. Example: `[[0,1],[1,0]]` → `2`
* **Key Constraints:** 1 ≤ n ≤ 100, return -1 if no path